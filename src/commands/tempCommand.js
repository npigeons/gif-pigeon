import { approved, storage } from '../globals.js';

export async function action(interaction) {
	// const msg = await storage.messages.fetch('1112833429671780453');
	// await msg.edit({ content: msg.content.replace('<@405639391118098432>', '<@167642756544462849>') });
}
